# Résolution npm cohérente local + CI (npm ci strict).
# Dette connue : @vitejs/plugin-react@6 réclame vite@^8 alors que vitest@2 cible
# vite@5/6 — conflit de peer dep que npm install tolère déjà. On l'assume
# explicitement ici pour que `npm ci` (CI) ne casse pas.
legacy-peer-deps=true
